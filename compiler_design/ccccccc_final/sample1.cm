int fib(int n)
{
    output(n);
    return fib(n*11);
}

void main(void)
{
    int a;
    int b;
    a = input();
    b = input();
    if (a > b)
        output(a);
    if (b > a)
        output(b);
    return;
}
